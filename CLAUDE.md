# Working conventions

- Never run `git push` (or otherwise publish commits to a remote) in this repo. The user pushes code themselves, always. Committing locally is fine when asked; leave pushing and deploying to the user.
